1. 派生类的构造函数的调用次序如下（析构函数的调用顺序与构造函数相反）：
（1）调用基类构造函数，调用顺序按照它们被继承时声明的顺序（从左向右）。
（2）调用成员对象的构造函数，调用顺序按照它们在类中声明的顺序。
（3）派生类的构造函数体中的内容。

定义4个类：分别是Point类、Shape类、三角形Triangle类和矩形Rectangle类。每个类都要有构造函数和析构函数，并在构造函数和析构函数里添加输出语句，用来显示是哪个类的构造函数或析构函数被调用了。
(1)Point类，数据成员int x, int y. 构造函数Point(int x, int y);
(2)Shape类，对象成员Point postion，构造函数Shape(int x, int y)（如何构造成员对象postion？）; 移动到新位置函数moveTo(int x, int y); 
(3)从Shape类派生出三角形Triangle类和矩形Rectangle类。为这两个类增加合适的数据成员和构造函数（如何调用基类的构造函数？），并增加一个函数float area() 用于计算图形的面积。
（4）编写合适的main函数，测试上述类的功能。并体会基类、派生类和成员对象的构造函数、析构函数的调用顺序。

2. 类的组合和继承有什么共同点和差异？通过组合生成的类与被组合的类之间的逻辑关系是什么？继承呢？

