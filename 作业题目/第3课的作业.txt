
1. 把第一次作业中的结构体 struct Stack_T 改成用类CStack实现。要求：

（1）把CreateStack和FreeStack改成构造函数和析构函数。
（2）其他函数改成类的成员函数。
（3) 在构造函数和析构函数里添加一条打印语句，体会构造函数和析构函数如何被自动调用。
（4）在main函数里使用CStack声明一个对象stack. 然后执行push和pop操作。


作业必须包含独立完成声明。

